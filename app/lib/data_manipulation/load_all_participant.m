function [experiment_data_struct] = load_all_participant(global_folder_path)
%LOAD_ALL_PARTICIPANT load the data from all participant into workspace
%   Detailed explanation goes here

% Will go through each folder and load each participant by calling
% load_participant. It will return experiment_data_struct which contain an
% array of data_struct.
end

