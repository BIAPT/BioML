function [data_struct] = load_participant(folder_path)
%LOAD_PARTICIPANT Summary load the CSV and JSON data from one participant
%   Detailed explanation goes here

% Will first go through all the files in the folder and look for the CSV
% file and the right JSON file. Then it will first load the CSV file and
% then it will append the JSON data into the data structure. It will then
% return that data structure.

end

